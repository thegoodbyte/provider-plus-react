import { api } from './api';
import { API_BASE_URL } from '../config/api.config';
export interface ClientFoodForm { _id: string; display_id: number; client_id: string; booking_id?: string; retreat_id?: string | { _id?: string; code?: string; retreatCode?: string; name?: string }; status: 'draft' | 'submitted' | 'reviewed'; language: 'en' | 'cs' | 'pl'; answers: Record<string, unknown>; email: string; signature_name: string; submitted_at?: string; pdf_file?: string; }
export const clientFoodFormsApi = { getAll: () => api.get<ClientFoodForm[]>('/client-food-forms'), getOne: (id: string) => api.get<ClientFoodForm>(`/client-food-forms/${id}`), pdfUrl: (id: string) => `${API_BASE_URL}/client-food-forms/${id}/pdf`, update: (id: string, data: Partial<ClientFoodForm>) => api.patch<ClientFoodForm>(`/client-food-forms/${id}`, data), delete: (id: string) => api.delete(`/client-food-forms/${id}`) };
